{ ... }:{

  # Import results hardware scan.
  imports = [
    ../hardware.nix
  ];

  # Bootloader
  boot.loader.grub.enable = true;
  boot.loader.grub.device = "/dev/sda";
  boot.loader.grub.useOSProber = true;
}